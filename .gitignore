# Biblioteca de teste gerada (dev/tools/gen_test_music.py) e dados do Navidrome de teste
dev/music/
dev/navidrome-data/

# Configurações locais do Claude Code
.claude/settings.local.json

# Python
__pycache__/
dist/

# Sobras de build que entraram sem querer (o .gitignore do app não alcança
# third_party/ nem o build do Gradle na raiz do módulo Android).
**/build/
**/.gradle/
**/.dart_tool/
app/rust/target/

# Ajudante local de uma vez só; leva a chave pública desta máquina.
dev/windows_ssh_setup.ps1
